
export interface User {
    avatar: string
    created: string
    email: string
    emailVisibility: boolean
    id: string
    name: string
    posts: string[]
    updated: string
    username: string
    verified: boolean
}